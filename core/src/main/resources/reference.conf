include "sc-akka.conf"

shadowcloud {
  default-storage {
    sync-interval = 15s
    index-compact-threshold = 1000
    chunk-key = hash
  }

  default-region {
    index-replication-factor = 0
    data-replication-factor = 1
  }

  storage {
    providers {
      local = com.karasiq.shadowcloud.storage.LocalStorageProvider
    }
  }

  crypto {
    hashing {
      default {
        algorithm = Blake2b
      }

      none {
        algorithm = ""
      }

      chunks = default
      chunks-encrypted = none
      files = none
      files-encrypted = none
    }

    encryption {
      default {
        algorithm = ChaCha20
        key-size = 256
      }

      none {
        algorithm = ""
        key-size = 0
      }

      chunks = default 
    }

    providers {
      no-op = com.karasiq.shadowcloud.crypto.NoOpCryptoProvider
    }
  }

  parallelism {
    hashing = 0
    encryption = 0
    write = 8
    read = 8
  }
}