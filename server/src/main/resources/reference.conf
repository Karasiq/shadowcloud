shadowcloud.http-server {
  host = 127.0.0.1
  port = 1911
  use-multipart-byte-ranges = true
}

akka.http.server {
  idle-timeout = 60m
  request-timeout = 60m 

  parsing {
    max-uri-length = 1M
    max-chunk-size = 20M
    max-content-length = 200M
  }
}